import greenfoot.*;

/**
 * Unit 3 project: tidy the bee up, and give it a score.
 *
 * Start from YOUR Unit 2 file, not from a blank one. By the end of Unit 2 the
 * whole game was inside act(), which worked and was already getting hard to
 * read. Lesson 3.1 is about exactly that feeling.
 *
 * Everything this asks for is from Unit 3:
 *   writing a void method           3.1 and 3.2
 *   calling it from act()           3.2
 *   an instance variable            3.8
 *
 * @author (your name)
 */
public class Bee extends Actor
{
    // TO DO (3.8): declare a private int called score, starting at 0.
    // It goes HERE, in the class body, not inside a method. Lesson 3.8 shows
    // what happens if you put it in the wrong place, and it is worth doing the
    // wrong version once on purpose so you recognise it later.

    /**
     * act() should end up being a list of names. Somebody reading it should be
     * able to tell what the game does without reading any of the details.
     */
    public void act()
    {
        // TO DO: call checkKeyPress();
    }

    /**
     * Moves the bee while an arrow key is held.
     */
    public void checkKeyPress()
    {
        // TO DO: move the four if statements you wrote in Unit 2 into here,
        // unchanged. This method is not new work; it is your old work with a
        // name on it.
    }
}
