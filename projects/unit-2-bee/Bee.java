import greenfoot.*;

/**
 * Unit 2 project: make the bee fly.
 *
 * This is the first file you fill in. It is also the file you will keep: the
 * bee gets tidied up in Unit 3 and learns to eat in Unit 4, so what you write
 * here is the start of the finished game, not a throwaway exercise.
 *
 * Everything this asks for is from Unit 2:
 *   if statements                   2.4
 *   Greenfoot.isKeyDown             2.7
 *   setLocation, getX, getY         1.4 and 1.5
 *
 * @author (your name)
 */
public class Bee extends Actor
{
    /**
     * Runs every frame. For now the whole game lives in here; that is fine,
     * and Unit 3 is about what to do when it stops being fine.
     */
    public void act()
    {
        // TO DO: check whether the "up" arrow is held down.
        // If it is, move the bee 5 pixels UP, which means a SMALLER y.
        // Use setLocation(getX(), getY() - 5).

        // TO DO: the same for "down", 5 pixels DOWN, which is a bigger y.

        // TO DO: the same for "left", 5 pixels left, which is a smaller x.

        // TO DO: the same for "right", 5 pixels right, which is a bigger x.
    }
}
