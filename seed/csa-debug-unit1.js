'use strict';
// ─────────────────────────────────────────────────────────────────────────────
//  AP CSA DEBUGGING EXERCISES - UNIT 1 (Using Objects and Methods).
//
//  Unit 1 students have the least debugging vocabulary of anyone in the course,
//  so these are deliberately the SHORTEST programs in the bank: two bugs each,
//  both named in the task, in twenty lines or fewer. The skill being built is
//  reading output against a spec, not hunting.
//
//  Three of these are the bugs that define the unit, and they are here on
//  purpose because none of them produces an error message:
//    1.6  x =+ 1 is legal Java and means x = +1
//    1.8  a line commented out by accident, deleting behavior silently
//    1.14 word.toUpperCase(); with the result thrown away, because Strings
//         are immutable and every String method RETURNS a new one
//
//  Zero PII: author content only. No em-dashes.
// ─────────────────────────────────────────────────────────────────────────────

const EXERCISES = [
  {
    lesson: '1.1', unit: 'unit-1',
    mode: 'program',
    name: 'Right Steps, Wrong Order',
    title: 'Introduction to Algorithms, Programming, and Compilers',
    brief: 'Every instruction in this program is correct on its own. The order they run in is not, and the total comes out wrong by a few cents in a way no compiler will ever mention. Sequencing is part of the algorithm, not a detail of how it is typed.',
    task: [
      'The discount is applied AFTER the tax is calculated, so tax is charged on the full price instead of on the discounted price. Reorder the statements so the discount comes off first and the tax is computed on what remains.',
      'The final total prints before the tax has been added to it, so it reports the pre-tax number. Move the printing to the end, after every calculation is finished.',
      'The arithmetic in each individual line is correct. Only the order changes.',
    ],
    reads: 'A price in cents, then a discount in cents.',
    prints: 'Three lines: the discounted price, the tax at 10 percent of the discounted price, and the final total.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int price = input.nextInt();',
      '    int discount = input.nextInt();',
      '',
      '    int tax = price * 10 / 100;',
      '    int discounted = price - discount;',
      '    int total = discounted;',
      '',
      '    System.out.println(discounted);',
      '    System.out.println(tax);',
      '    System.out.println(total);',
      '',
      '    total = discounted + tax;',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int price = input.nextInt();',
      '    int discount = input.nextInt();',
      '',
      '    int discounted = price - discount;',
      '    int tax = discounted * 10 / 100;',
      '    int total = discounted + tax;',
      '',
      '    System.out.println(discounted);',
      '    System.out.println(tax);',
      '    System.out.println(total);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'A statement can only use values that already exist when it runs. The tax line runs before discounted has been computed, so it has nothing to work from except the full price.',
      'The last line of the starter computes the correct total, and then the program ends without printing it. Assigning a value after you have already printed the variable changes nothing anyone can see.',
      'Write the three quantities in dependency order before you touch the code: discounted depends on price, tax depends on discounted, total depends on both. Statements have to follow that order.',
    ],
    seo: 'AP CSA 1.1 debugging exercise: reorder algorithm steps so a discount applies before tax and the total prints after it is computed.',
    cases: [
      { stdin: '1000 200\n', hidden: 0 },
      { stdin: '500 0\n', hidden: 0 },
      { stdin: '2500 500\n', hidden: 1 },
      { stdin: '999 99\n', hidden: 1 },
      { stdin: '100 100\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.2', unit: 'unit-1',
    mode: 'program',
    name: 'The Wrong Container',
    title: 'Variables and Data Types',
    brief: 'An int cannot hold half of anything. Store a measurement that has a fractional part in one and the fraction is gone before you get a chance to print it, silently and permanently. Choosing the type is choosing what the program is able to remember.',
    task: [
      'The average is declared as an int, so it throws away the fractional part of every measurement. Declare it as a double and compute it with double division so the fraction survives.',
      'The running total is reset to 0 in the middle of the additions, so only the last two values are counted. Remove the stray reset.',
      'The count and the reading loop are correct.',
    ],
    reads: 'Exactly four integer measurements.',
    prints: 'Two lines: the total of the four values, then their average as a double.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '    int d = input.nextInt();',
      '',
      '    int total = 0;',
      '    total = total + a;',
      '    total = total + b;',
      '    total = 0;',
      '    total = total + c;',
      '    total = total + d;',
      '',
      '    int average = total / 4;',
      '',
      '    System.out.println(total);',
      '    System.out.println(average);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '    int d = input.nextInt();',
      '',
      '    int total = 0;',
      '    total = total + a;',
      '    total = total + b;',
      '    total = total + c;',
      '    total = total + d;',
      '',
      '    double average = total / 4.0;',
      '',
      '    System.out.println(total);',
      '    System.out.println(average);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Declaring average as a double is only half the fix. total / 4 is int divided by int, which truncates BEFORE the result is ever stored, so a double variable receives an already-damaged number. Divide by 4.0 to force real division.',
      'A double prints with a decimal point, so 7 becomes 7.0 in the output. That is expected and correct here.',
      'The reset in the middle is the kind of line that survives a copy and paste. Trace total after each statement and it is obvious where the first two values went.',
    ],
    seo: 'AP CSA 1.2 debugging exercise: use a double for an average that loses its fraction and remove a total reset midway through.',
    cases: [
      { stdin: '1 2 3 4\n', hidden: 0 },
      { stdin: '10 20 30 40\n', hidden: 0 },
      { stdin: '7 7 7 7\n', hidden: 1 },
      { stdin: '0 0 0 1\n', hidden: 1 },
      { stdin: '-2 4 -6 8\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.3', unit: 'unit-1',
    mode: 'program',
    name: 'Precedence Without Parentheses',
    title: 'Expressions and Assignment Statements',
    brief: 'Multiplication happens before addition whether or not that is what you meant. The expression reads left to right on the page and does not execute that way, and the only thing standing between the two readings is a pair of parentheses nobody typed.',
    task: [
      'The average of three values is written as a + b + c / 3, which divides only c by 3 because division binds tighter than addition. Add the parentheses that make the whole sum divide.',
      'The area of the border is written as width * 2 + height * 2 but the spec calls for twice the sum of width and height plus four corners. Fix the expression so it matches the stated formula.',
      'The remainder line is correct.',
    ],
    reads: 'Three integers a, b and c, then a width and a height.',
    prints: 'Three lines: the integer average of the three values, a modulo b, and the border count.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '    int width = input.nextInt();',
      '    int height = input.nextInt();',
      '',
      '    int average = a + b + c / 3;',
      '    System.out.println(average);',
      '',
      '    System.out.println(a % b);',
      '',
      '    int border = width * 2 + height * 2;',
      '    System.out.println(border);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '    int width = input.nextInt();',
      '    int height = input.nextInt();',
      '',
      '    int average = (a + b + c) / 3;',
      '    System.out.println(average);',
      '',
      '    System.out.println(a % b);',
      '',
      '    int border = 2 * (width + height) + 4;',
      '    System.out.println(border);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Evaluate a + b + c / 3 with a = 3, b = 3, c = 3 by hand. Division first gives 3 + 3 + 1, which is 7, not 3. Parentheses are the only way to make the addition happen first.',
      'The border formula in the spec is twice the sum, plus four. The starter computes twice each side and never adds the corners, so it is short by exactly 4 on every input.',
      'When an expression mixes operators, write the intended order in words first, then add parentheses until the code matches the words. Extra parentheses cost nothing.',
    ],
    seo: 'AP CSA 1.3 debugging exercise: add parentheses so an average divides the whole sum and fix a border formula missing its corners.',
    cases: [
      { stdin: '3 3 3 2 5\n', hidden: 0 },
      { stdin: '10 4 7 3 3\n', hidden: 0 },
      { stdin: '9 9 9 1 1\n', hidden: 1 },
      { stdin: '7 2 3 10 20\n', hidden: 1 },
      { stdin: '1 1 1 0 0\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.4', unit: 'unit-1',
    mode: 'program',
    name: 'Reading in the Wrong Order',
    title: 'Assignment Statements and Input',
    brief: 'Input arrives in one fixed order and the program consumes it in another, so every value lands in the wrong variable. Nothing crashes, the types all match, and the output is confidently mislabeled.',
    task: [
      'The spec says the quantity comes first and the price second, but the program reads price first. Swap the two reads so each variable receives the value it is named for.',
      'The subtotal is assigned before the values have been read, so it uses the default 0. Move the assignment below the reads.',
      'The printing at the bottom is correct and should not move.',
    ],
    reads: 'A quantity, then a unit price in cents.',
    prints: 'Three lines: the quantity, the unit price, and the subtotal.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '',
      '    int quantity = 0;',
      '    int price = 0;',
      '    int subtotal = quantity * price;',
      '',
      '    price = input.nextInt();',
      '    quantity = input.nextInt();',
      '',
      '    System.out.println(quantity);',
      '    System.out.println(price);',
      '    System.out.println(subtotal);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '',
      '    int quantity = input.nextInt();',
      '    int price = input.nextInt();',
      '    int subtotal = quantity * price;',
      '',
      '    System.out.println(quantity);',
      '    System.out.println(price);',
      '    System.out.println(subtotal);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'nextInt does not know what the number means. It hands the next number to whichever variable is on the left of the assignment, so the ORDER of your reads has to match the order of the input exactly.',
      'An assignment computes its right side once, at the moment it runs. Writing subtotal = quantity * price before the reads captures 0 times 0 and never updates itself later.',
      'Test with a quantity and price that are different numbers. Using 5 and 5 makes a swapped read look perfectly correct.',
    ],
    seo: 'AP CSA 1.4 debugging exercise: fix Scanner reads taken in the wrong order and a subtotal computed before its inputs exist.',
    cases: [
      { stdin: '3 250\n', hidden: 0 },
      { stdin: '1 99\n', hidden: 0 },
      { stdin: '10 5\n', hidden: 1 },
      { stdin: '7 7\n', hidden: 1 },
      { stdin: '0 500\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.5', unit: 'unit-1',
    mode: 'program',
    name: 'Casting Truncates, It Does Not Round',
    title: 'Casting and Range of Values',
    brief: 'A cast to int does not round to the nearest whole number. It deletes everything after the decimal point, so 3.99 becomes 3. Code that assumes rounding is off by one on almost every input that has a fraction at all.',
    task: [
      'The rounded value uses a plain cast to int, which truncates. To round to the nearest whole number, add 0.5 before casting. Fix it.',
      'The halved value casts to int BEFORE dividing, so the fraction is destroyed before the division can produce one. Divide as doubles and cast only if you need to.',
      'The printed original value is correct.',
    ],
    reads: 'One double value.',
    prints: 'Three lines: the value as given, the value rounded to the nearest whole number, and half the value as a double.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    double value = input.nextDouble();',
      '',
      '    System.out.println(value);',
      '',
      '    int rounded = (int) value;',
      '    System.out.println(rounded);',
      '',
      '    double half = (int) value / 2;',
      '    System.out.println(half);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    double value = input.nextDouble();',
      '',
      '    System.out.println(value);',
      '',
      '    int rounded = (int) (value + 0.5);',
      '    System.out.println(rounded);',
      '',
      '    double half = value / 2;',
      '    System.out.println(half);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Cast 3.99 to int and you get 3, not 4. Truncation always moves toward zero, so adding 0.5 first is what turns it into rounding: 3.99 + 0.5 is 4.49, which truncates to 4.',
      'In the halving line the cast applies to value alone, so an int is divided by an int and the result is an int before it is ever widened back to double. Removing the cast entirely is the fix.',
      'Parentheses decide what a cast applies to. (int) value + 0.5 casts first and then adds. (int) (value + 0.5) adds first and then casts. Only the second one rounds.',
    ],
    seo: 'AP CSA 1.5 debugging exercise: make a cast round instead of truncate and stop an int cast destroying a fraction before division.',
    cases: [
      { stdin: '3.99\n', hidden: 0 },
      { stdin: '7.0\n', hidden: 0 },
      { stdin: '2.5\n', hidden: 1 },
      { stdin: '10.4\n', hidden: 1 },
      { stdin: '0.6\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.6', unit: 'unit-1',
    mode: 'program',
    name: 'The Operator That Is Two Operators',
    title: 'Compound Assignment Operators',
    brief: 'x =+ 1 compiles. It is not a typo the compiler will catch, because it is a perfectly ordinary assignment of positive one, and it silently replaces the value instead of adding to it. Two characters in the wrong order change accumulation into overwriting.',
    task: [
      'The running total uses total =+ value, which assigns the value rather than adding it, so the total ends up holding only the last number. Change it to the compound operator that adds.',
      'The countdown uses count =- 1, which assigns negative one every time instead of subtracting. Change it to the compound operator that subtracts.',
      'The multiply-by-two line already uses a compound operator correctly. Use it as your model.',
    ],
    reads: 'Three integers.',
    prints: 'Three lines: the sum of the three values, the value 10 reduced by 1 three times, and the third value doubled.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '',
      '    int total = 0;',
      '    total =+ a;',
      '    total =+ b;',
      '    total =+ c;',
      '    System.out.println(total);',
      '',
      '    int count = 10;',
      '    count =- 1;',
      '    count =- 1;',
      '    count =- 1;',
      '    System.out.println(count);',
      '',
      '    int doubled = c;',
      '    doubled *= 2;',
      '    System.out.println(doubled);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '',
      '    int total = 0;',
      '    total += a;',
      '    total += b;',
      '    total += c;',
      '    System.out.println(total);',
      '',
      '    int count = 10;',
      '    count -= 1;',
      '    count -= 1;',
      '    count -= 1;',
      '    System.out.println(count);',
      '',
      '    int doubled = c;',
      '    doubled *= 2;',
      '    System.out.println(doubled);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Read total =+ a as total = (+a). The plus is a unary sign on a, not part of the assignment. So the statement means "replace total with a", and after three of them total holds only c.',
      'Similarly count =- 1 means count = (-1), which is why the countdown from 10 lands on -1 instead of 7 no matter how many times it runs.',
      'The compound operator always has the symbol FIRST and the equals second: += and -=. If the equals comes first, you have written a plain assignment with a signed value.',
    ],
    seo: 'AP CSA 1.6 debugging exercise: fix the classic Java trap where x =+ 1 assigns instead of adding, and x =- 1 instead of subtracting.',
    cases: [
      { stdin: '1 2 3\n', hidden: 0 },
      { stdin: '10 20 30\n', hidden: 0 },
      { stdin: '5 5 5\n', hidden: 1 },
      { stdin: '-1 -2 -3\n', hidden: 1 },
      { stdin: '0 0 7\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.7', unit: 'unit-1',
    mode: 'program',
    name: 'Reading the Documentation Wrong',
    title: 'API and Libraries',
    brief: 'A library method does exactly what its documentation says, which is rarely exactly what you assumed. Math.pow hands back a double even when both arguments look like whole numbers, and Math.max takes two arguments, not three.',
    task: [
      'Math.pow returns a double, so printing it directly gives 8.0 where the spec asks for 8. Cast the result to int.',
      'The largest of three values is attempted with a single Math.max call given a nested comparison that returns a boolean. Use two nested Math.max calls instead.',
      'The absolute value line is correct.',
    ],
    reads: 'Three integers: a base, an exponent, and a third value.',
    prints: 'Three lines: the base raised to the exponent as a whole number, the absolute value of the third value, and the largest of the three inputs.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int base = input.nextInt();',
      '    int exponent = input.nextInt();',
      '    int third = input.nextInt();',
      '',
      '    System.out.println(Math.pow(base, exponent));',
      '',
      '    System.out.println(Math.abs(third));',
      '',
      '    int largest = Math.max(base, exponent);',
      '    System.out.println(largest);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int base = input.nextInt();',
      '    int exponent = input.nextInt();',
      '    int third = input.nextInt();',
      '',
      '    System.out.println((int) Math.pow(base, exponent));',
      '',
      '    System.out.println(Math.abs(third));',
      '',
      '    int largest = Math.max(Math.max(base, exponent), third);',
      '    System.out.println(largest);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Math.pow is declared to take two doubles and return a double. Java widens your ints on the way in without complaint, and the answer comes back as 8.0 rather than 8. A cast to int is the whole fix.',
      'Math.max compares exactly two values. To compare three, feed the result of one call into another: Math.max(Math.max(a, b), c). The starter simply ignores the third value.',
      'When a library method surprises you, the fastest fix is to read its signature: what types does it take, and what type does it hand back? Both bugs here are answered by that question alone.',
    ],
    seo: 'AP CSA 1.7 debugging exercise: cast the double returned by Math.pow and nest Math.max calls to compare three values.',
    cases: [
      { stdin: '2 3 -5\n', hidden: 0 },
      { stdin: '5 2 100\n', hidden: 0 },
      { stdin: '3 3 1\n', hidden: 1 },
      { stdin: '10 1 -20\n', hidden: 1 },
      { stdin: '2 0 0\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.8', unit: 'unit-1',
    mode: 'program',
    name: 'Commented Out by Accident',
    title: 'Documentation with Comments',
    brief: 'A comment is invisible to the compiler, which is the entire point and also the danger. A line that got commented out during some earlier debugging session is still sitting there, perfectly readable to you and completely absent from the program.',
    task: [
      'The line that adds the bonus to the total is commented out, so the bonus is read and never used. Uncomment it.',
      'A block comment that was opened to explain the tax calculation was never closed properly, so it swallows the tax line as well. Close the comment before the tax calculation so that line runs again.',
      'Do not delete the comments themselves. Documentation is worth keeping; it just has to stop eating the code.',
    ],
    reads: 'A base amount, then a bonus.',
    prints: 'Three lines: the base plus bonus, the tax at 10 percent of that, and the final total.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int base = input.nextInt();',
      '    int bonus = input.nextInt();',
      '',
      '    int subtotal = base;',
      '    // subtotal = subtotal + bonus;',
      '    System.out.println(subtotal);',
      '',
      '    /* The tax is ten percent, computed on the subtotal',
      '       after the bonus has been included.',
      '    int tax = subtotal * 10 / 100;',
      '    */',
      '    int tax = 0;',
      '    System.out.println(tax);',
      '',
      '    System.out.println(subtotal + tax);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int base = input.nextInt();',
      '    int bonus = input.nextInt();',
      '',
      '    int subtotal = base;',
      '    subtotal = subtotal + bonus;',
      '    System.out.println(subtotal);',
      '',
      '    /* The tax is ten percent, computed on the subtotal',
      '       after the bonus has been included. */',
      '    int tax = subtotal * 10 / 100;',
      '    System.out.println(tax);',
      '',
      '    System.out.println(subtotal + tax);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Two slashes disable everything to the end of that line. The bonus line reads like working code and contributes nothing, which is why the subtotal always equals the base exactly.',
      'A block comment runs from its opening to the FIRST closing marker, crossing as many lines as it needs to. Here it opened before the explanation and stayed open through the tax calculation, so that line is prose as far as Java is concerned.',
      'Once the real tax line is live you no longer need the placeholder that sets tax to 0, because you cannot declare tax twice in the same scope.',
    ],
    seo: 'AP CSA 1.8 debugging exercise: restore a line disabled by a line comment and close a block comment swallowing the tax calculation.',
    cases: [
      { stdin: '1000 200\n', hidden: 0 },
      { stdin: '500 0\n', hidden: 0 },
      { stdin: '250 250\n', hidden: 1 },
      { stdin: '99 1\n', hidden: 1 },
      { stdin: '10000 5000\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.9', unit: 'unit-1',
    mode: 'program',
    name: 'Arguments in the Wrong Slots',
    title: 'Method Signatures',
    brief: 'A method signature is a contract about order as much as about types. Hand it two ints in the wrong order and every type checks out, the call compiles, and the answer is quietly the answer to a different question.',
    task: [
      'The call to describe passes the height first and the width second, but the signature declares width first. Swap the arguments at the CALL, not in the method.',
      'The percent method divides part by whole in the wrong order, so it reports the reciprocal, which is 0 for any real input under integer division. Fix the order inside the method.',
      'The signatures themselves are correct and must not change. Only the call and the body of percent need work.',
    ],
    reads: 'A width, a height, then a part and a whole.',
    prints: 'Two lines: the area described as width times height, then the part as an integer percentage of the whole.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static int describe(int width, int height) {',
      '    return width * height;',
      '  }',
      '',
      '  public static int percent(int part, int whole) {',
      '    return whole * 100 / part;',
      '  }',
      '',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int width = input.nextInt();',
      '    int height = input.nextInt();',
      '    int part = input.nextInt();',
      '    int whole = input.nextInt();',
      '',
      '    System.out.println(describe(height, width));',
      '    System.out.println(percent(part, whole));',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static int describe(int width, int height) {',
      '    return width * height;',
      '  }',
      '',
      '  public static int percent(int part, int whole) {',
      '    return part * 100 / whole;',
      '  }',
      '',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int width = input.nextInt();',
      '    int height = input.nextInt();',
      '    int part = input.nextInt();',
      '    int whole = input.nextInt();',
      '',
      '    System.out.println(describe(width, height));',
      '    System.out.println(percent(part, whole));',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Multiplication is commutative, so the area happens to come out right anyway. That is exactly what makes this dangerous: the same swapped call in a method that subtracts or divides would be visibly wrong, and here it is invisible. Fix it because the contract says so.',
      'In percent, ask what fraction you want: part out of whole. Multiplying by 100 first keeps the integer division from collapsing to 0, but the part still has to be on top.',
      'Parameter names are documentation for the caller. When a call site reads describe(height, width) against a signature that says (width, height), the call is wrong even if the result looks fine.',
    ],
    seo: 'AP CSA 1.9 debugging exercise: fix arguments passed in the wrong order at a call site and an inverted percentage calculation.',
    cases: [
      { stdin: '3 4 25 50\n', hidden: 0 },
      { stdin: '5 5 1 4\n', hidden: 0 },
      { stdin: '10 2 3 3\n', hidden: 1 },
      { stdin: '7 1 99 100\n', hidden: 1 },
      { stdin: '2 8 1 1000\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.10', unit: 'unit-1',
    mode: 'program',
    name: 'Ignoring What Came Back',
    title: 'Calling Class Methods',
    brief: 'A method that returns a value has done nothing useful until somebody catches it. Call it as a bare statement and the answer is computed, handed back, and dropped, leaving the original variable exactly as it was.',
    task: [
      'The call to twice is written as a bare statement, so the doubled value is computed and discarded and the variable never changes. Assign the result back.',
      'The call to clamp passes its arguments but its return value is printed without ever being used to update the value, so the later line prints the unclamped number. Store the result before printing it.',
      'Both method bodies are correct. Everything to fix is at the call sites.',
    ],
    reads: 'A value, then a maximum.',
    prints: 'Three lines: the value doubled, that doubled value clamped to the maximum, and the clamped value printed again from the variable.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static int twice(int n) {',
      '    return n * 2;',
      '  }',
      '',
      '  public static int clamp(int n, int max) {',
      '    if (n > max) {',
      '      return max;',
      '    }',
      '    return n;',
      '  }',
      '',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int value = input.nextInt();',
      '    int max = input.nextInt();',
      '',
      '    twice(value);',
      '    System.out.println(value);',
      '',
      '    System.out.println(clamp(value, max));',
      '    System.out.println(value);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static int twice(int n) {',
      '    return n * 2;',
      '  }',
      '',
      '  public static int clamp(int n, int max) {',
      '    if (n > max) {',
      '      return max;',
      '    }',
      '    return n;',
      '  }',
      '',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int value = input.nextInt();',
      '    int max = input.nextInt();',
      '',
      '    value = twice(value);',
      '    System.out.println(value);',
      '',
      '    value = clamp(value, max);',
      '    System.out.println(value);',
      '    System.out.println(value);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Passing an int to a method passes a COPY of it. Nothing the method does to its parameter can reach the variable you passed in, so the only way the answer gets back to you is the return value.',
      'twice(value); on its own line is legal Java and does real work whose result nobody keeps. If a method returns something and you are not storing or printing it, ask yourself why you called it.',
      'The third printed line must come from the variable, not from another call, which is how the exercise checks that you actually stored the clamped result.',
    ],
    seo: 'AP CSA 1.10 debugging exercise: assign the return value of a method call instead of discarding it as a bare statement.',
    cases: [
      { stdin: '5 100\n', hidden: 0 },
      { stdin: '60 100\n', hidden: 0 },
      { stdin: '1 1\n', hidden: 1 },
      { stdin: '50 10\n', hidden: 1 },
      { stdin: '0 5\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.11', unit: 'unit-1',
    mode: 'program',
    name: 'Integer Division Inside the Formula',
    title: 'Math Class',
    brief: 'Math.sqrt and Math.pow work in doubles and are not the problem. The problem is the int division sitting inside the expression handed to them, which has already destroyed the fraction before the library method is ever called.',
    task: [
      'The average passed into Math.sqrt is computed with int division, so the fraction is gone before the square root sees it. Force the division to produce a double.',
      'The hypotenuse squares its sides with a * a and b * b, which is correct, but then takes the square root of their DIFFERENCE instead of their sum. Fix the operator.',
      'The Math.abs call is correct.',
    ],
    reads: 'Two positive integers a and b.',
    prints: 'Three lines: the square root of the average of a and b, the hypotenuse of a right triangle with legs a and b, and the absolute difference between a and b.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '',
      '    System.out.println(Math.sqrt((a + b) / 2));',
      '',
      '    System.out.println(Math.sqrt(a * a - b * b));',
      '',
      '    System.out.println(Math.abs(a - b));',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '',
      '    System.out.println(Math.sqrt((a + b) / 2.0));',
      '',
      '    System.out.println(Math.sqrt(a * a + b * b));',
      '',
      '    System.out.println(Math.abs(a - b));',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'With a = 3 and b = 4 the average is 3.5, but (3 + 4) / 2 in ints is 3. Math.sqrt then dutifully computes the square root of the wrong number. Dividing by 2.0 keeps the arithmetic in doubles.',
      'A method that accepts a double will happily accept an int, widening it silently. That is why nothing warns you: the damage happened before the call, in your own expression.',
      'For a right triangle the hypotenuse is the square root of the SUM of the squares. Subtracting gives a negative under the root whenever b is larger than a, and Math.sqrt of a negative is NaN.',
    ],
    seo: 'AP CSA 1.11 debugging exercise: stop integer division truncating inside a Math.sqrt call and fix a hypotenuse using subtraction.',
    cases: [
      { stdin: '3 4\n', hidden: 0 },
      { stdin: '6 8\n', hidden: 0 },
      { stdin: '1 1\n', hidden: 1 },
      { stdin: '5 12\n', hidden: 1 },
      { stdin: '9 2\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.12', unit: 'unit-1',
    mode: 'program',
    name: 'Two Names, One Object',
    title: 'Objects: Instances of Classes',
    brief: 'Assigning one object variable to another does not make a second object. It makes a second name for the first one, and a change made through either name is visible through both, because there was only ever one thing there.',
    task: [
      'The copy is made by assigning one reference to another, so both names point at the same Counter and incrementing one appears to increment both. Build a genuinely separate object instead.',
      'The second counter is never actually incremented before it is printed, because the increment call is made on the first counter twice. Fix the second call so it targets the copy.',
      'The Counter class itself is correct and should not change.',
    ],
    reads: 'Two integers: a starting value, and how many times to increment the first counter.',
    prints: 'Two lines: the first counter after its increments, then the second counter, which should still hold its starting value plus one.',
    starter: [
      'import java.util.Scanner;',
      '',
      'class Counter {',
      '  private int value;',
      '',
      '  public Counter(int value) {',
      '    this.value = value;',
      '  }',
      '',
      '  public void increment() {',
      '    value++;',
      '  }',
      '',
      '  public int getValue() {',
      '    return value;',
      '  }',
      '}',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int start = input.nextInt();',
      '    int times = input.nextInt();',
      '',
      '    Counter first = new Counter(start);',
      '    Counter second = first;',
      '',
      '    for (int i = 0; i < times; i++) {',
      '      first.increment();',
      '    }',
      '    first.increment();',
      '',
      '    System.out.println(first.getValue());',
      '    System.out.println(second.getValue());',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'class Counter {',
      '  private int value;',
      '',
      '  public Counter(int value) {',
      '    this.value = value;',
      '  }',
      '',
      '  public void increment() {',
      '    value++;',
      '  }',
      '',
      '  public int getValue() {',
      '    return value;',
      '  }',
      '}',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int start = input.nextInt();',
      '    int times = input.nextInt();',
      '',
      '    Counter first = new Counter(start);',
      '    Counter second = new Counter(start);',
      '',
      '    for (int i = 0; i < times; i++) {',
      '      first.increment();',
      '    }',
      '    second.increment();',
      '',
      '    System.out.println(first.getValue());',
      '    System.out.println(second.getValue());',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'An object variable holds a reference, which you can picture as an arrow pointing at the object. Counter second = first; copies the arrow, not the thing it points at. Now two arrows aim at one Counter.',
      'The only thing that creates a new object is the keyword new. If new appears once, exactly one object exists no matter how many variables are naming it.',
      'The starter increments first one extra time and never touches second at all, which on the same shared object looks almost right. Once the objects are separate, that second call has to target the second counter.',
    ],
    seo: 'AP CSA 1.12 debugging exercise: create a separate object instead of copying a reference so two counters stop sharing state.',
    cases: [
      { stdin: '0 3\n', hidden: 0 },
      { stdin: '10 1\n', hidden: 0 },
      { stdin: '5 0\n', hidden: 1 },
      { stdin: '100 5\n', hidden: 1 },
      { stdin: '-3 2\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.13', unit: 'unit-1',
    mode: 'program',
    name: 'Rebuilt Every Time Around',
    title: 'Object Creation',
    brief: 'The object is constructed inside the loop, so each pass throws away the one built on the previous pass and starts over from scratch. The accumulated total is only ever one item deep, and the loop looks completely reasonable.',
    task: [
      'The Basket is created inside the loop, so every pass replaces it with a brand new empty one and only the last item survives. Move the construction above the loop so one basket collects everything.',
      'The constructor is called without new on the second line, which means the variable is reassigned to a value from a method call rather than a fresh object. Restore the new keyword.',
      'The Basket class is correct.',
    ],
    reads: 'A count, then that many item prices.',
    prints: 'Two lines: the number of items in the basket and the total of their prices.',
    starter: [
      'import java.util.Scanner;',
      '',
      'class Basket {',
      '  private int count;',
      '  private int total;',
      '',
      '  public Basket() {',
      '    count = 0;',
      '    total = 0;',
      '  }',
      '',
      '  public void add(int price) {',
      '    count++;',
      '    total = total + price;',
      '  }',
      '',
      '  public int getCount() {',
      '    return count;',
      '  }',
      '',
      '  public int getTotal() {',
      '    return total;',
      '  }',
      '}',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    Basket basket = new Basket();',
      '    for (int i = 0; i < n; i++) {',
      '      basket = new Basket();',
      '      basket.add(input.nextInt());',
      '    }',
      '',
      '    System.out.println(basket.getCount());',
      '    System.out.println(basket.getTotal());',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'class Basket {',
      '  private int count;',
      '  private int total;',
      '',
      '  public Basket() {',
      '    count = 0;',
      '    total = 0;',
      '  }',
      '',
      '  public void add(int price) {',
      '    count++;',
      '    total = total + price;',
      '  }',
      '',
      '  public int getCount() {',
      '    return count;',
      '  }',
      '',
      '  public int getTotal() {',
      '    return total;',
      '  }',
      '}',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    Basket basket = new Basket();',
      '    for (int i = 0; i < n; i++) {',
      '      basket.add(input.nextInt());',
      '    }',
      '',
      '    System.out.println(basket.getCount());',
      '    System.out.println(basket.getTotal());',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Ask how many baskets should exist while the program runs. The answer is one, so new Basket() should appear exactly once, and never inside a loop that runs many times.',
      'The starter always reports a count of 1, because the basket printed at the end was created on the final pass and has had exactly one item added to it.',
      'An object created on an earlier pass is not saved anywhere once the variable is pointed at a new one. It is simply gone, along with everything that was added to it.',
    ],
    seo: 'AP CSA 1.13 debugging exercise: move object creation outside a loop so one object accumulates instead of being rebuilt each pass.',
    cases: [
      { stdin: '3\n100 200 300\n', hidden: 0 },
      { stdin: '1\n50\n', hidden: 0 },
      { stdin: '5\n1 2 3 4 5\n', hidden: 1 },
      { stdin: '4\n25 25 25 25\n', hidden: 1 },
      { stdin: '2\n0 999\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.14', unit: 'unit-1',
    mode: 'program',
    name: 'Strings Never Change',
    title: 'Calling Instance Methods',
    brief: 'toUpperCase does not shout at the String you called it on. It builds a brand new String and hands it back, leaving the original exactly as it was, because a String in Java can never be modified once it exists. Every String method returns; none of them edit.',
    task: [
      'The uppercase conversion is called as a bare statement, so the new String it produced is discarded and the original is printed unchanged. Assign the result back.',
      'The trim and the replace are chained but the chain is also discarded. Capture the result of the whole chain into the variable.',
      'The length line is correct, and it is worth noticing that length is the one call here that does not try to change anything.',
    ],
    reads: 'A single word, possibly with surrounding spaces on its line.',
    prints: 'Three lines: the word in uppercase, the word with every letter a replaced by the digit 4, and the length of the original word.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '',
      '    String shouted = word;',
      '    shouted.toUpperCase();',
      '    System.out.println(shouted);',
      '',
      '    String leet = word;',
      '    leet.replace("a", "4");',
      '    System.out.println(leet);',
      '',
      '    System.out.println(word.length());',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '',
      '    String shouted = word;',
      '    shouted = shouted.toUpperCase();',
      '    System.out.println(shouted);',
      '',
      '    String leet = word;',
      '    leet = leet.replace("a", "4");',
      '    System.out.println(leet);',
      '',
      '    System.out.println(word.length());',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Strings are immutable, which is a promise Java makes and never breaks: the characters inside an existing String cannot be altered by anything. So toUpperCase has no way to change your String even in principle. It can only build a new one and return it.',
      'shouted.toUpperCase(); computes the uppercase version, hands it back, and nobody catches it, so it is discarded immediately. The pattern you want is always s = s.someMethod().',
      'This is the same lesson as 1.10, on an object instead of an int. If a method returns a value, ignoring the return value ignores the entire point of the call.',
    ],
    seo: 'AP CSA 1.14 debugging exercise: assign the result of String methods because Strings are immutable and toUpperCase returns a new String.',
    cases: [
      { stdin: 'banana\n', hidden: 0 },
      { stdin: 'Java\n', hidden: 0 },
      { stdin: 'aaa\n', hidden: 1 },
      { stdin: 'xyz\n', hidden: 1 },
      { stdin: 'Alabama\n', hidden: 1 },
    ],
  },

  {
    lesson: '1.15', unit: 'unit-1',
    mode: 'program',
    name: 'Plus Means Two Different Things',
    title: 'String Manipulation',
    brief: 'The plus operator adds numbers and joins text, and which one it does depends entirely on what is to its left as it works across the line. Start with text and every number after it becomes text too, one at a time, left to right.',
    task: [
      'The label line prints the two numbers glued together as text instead of added, because a String appears before them and turns the pluses into joins. Add parentheses so the addition happens first.',
      'The last character is taken with charAt(word.length()), which is one past the end and throws StringIndexOutOfBoundsException. Fix the index.',
      'The first-character line is correct.',
    ],
    reads: 'A word, then two integers.',
    prints: 'Four lines: the first character of the word, the last character of the word, the sum of the two integers labeled with the prefix Sum: and the two integers joined as text with no label.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '',
      '    System.out.println(word.charAt(0));',
      '    System.out.println(word.charAt(word.length()));',
      '',
      '    System.out.println("Sum: " + a + b);',
      '    System.out.println("" + a + b);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '',
      '    System.out.println(word.charAt(0));',
      '    System.out.println(word.charAt(word.length() - 1));',
      '',
      '    System.out.println("Sum: " + (a + b));',
      '    System.out.println("" + a + b);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Java works left to right. "Sum: " + a is text, and then that text + b is more text, so with a = 2 and b = 3 you get Sum: 23. Parentheses around a + b force the addition to finish before anything is joined.',
      'The last line is supposed to join rather than add, and it already does. Leave it alone: it is there so you can see both behaviors of the same operator side by side.',
      'charAt uses the same index rules as everything else: valid positions run from 0 to length minus 1. The last character is always at length() - 1.',
    ],
    seo: 'AP CSA 1.15 debugging exercise: parenthesize addition before string concatenation and fix a charAt index one past the end.',
    cases: [
      { stdin: 'hello 2 3\n', hidden: 0 },
      { stdin: 'a 10 20\n', hidden: 0 },
      { stdin: 'zebra 0 0\n', hidden: 1 },
      { stdin: 'code 7 -7\n', hidden: 1 },
      { stdin: 'xy 100 1\n', hidden: 1 },
    ],
  },
];

module.exports = { EXERCISES };
