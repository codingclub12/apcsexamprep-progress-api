'use strict';
// ─────────────────────────────────────────────────────────────────────────────
//  AP CSA DEBUGGING EXERCISES - UNIT 2 (Selection and Iteration).
//
//  Split out by unit, matching the seed/csa-exercises/unitN.js pattern, because
//  one flat array across 53 lessons is not a file anyone edits confidently.
//  seed/csa-debug-exercises.js concatenates these; the shape contract, the
//  REQUIRED field check and the verifier are all unchanged.
//
//  AUTHORING RULE FOR THIS FILE
//  The planted bug is never a typo and never a compile error. It is the mistake
//  a real student makes on THIS topic, left in code that runs and looks
//  reasonable. A bug that stops the program from compiling teaches nothing
//  except how to read javac, and a bug that does not change the output is not a
//  bug. scripts/verify-csa-debug-exercises.js enforces the second half of that:
//  it proves the starter fails at least one case before the expected outputs are
//  written from the reference.
//
//  NO INFINITE LOOPS. A while-loop lesson tempts you to plant a missing update
//  and call it "the loop never ends". The verifier runs the starter with a
//  15 second timeout, and a hang is indistinguishable from a broken harness.
//  Loop bugs here are off-by-one and wrong-accumulator instead, which is also
//  what the exam actually tests.
//
//  Zero PII: author content only. No em-dashes.
// ─────────────────────────────────────────────────────────────────────────────

const EXERCISES = [
  {
    lesson: '2.1', unit: 'unit-2',
    mode: 'program',
    name: 'The Swap That Loses a Value',
    title: 'Algorithms with Selection and Repetition',
    brief: 'Two bugs, and neither one crashes. The program runs, prints something that looks plausible, and is wrong twice. That is the normal case in real code, and it is why reading output carefully matters more than watching for red text.',
    task: [
      'Run the program. The two values print swapped incorrectly: both lines show the same number. The swap overwrites one value before it has been saved anywhere. Fix it so both original values survive.',
      'The countdown stops one number early. It should count all the way down to 1 before LIFTOFF, and right now the last number never prints. Fix the loop condition.',
      'The LIFTOFF line and the way the values are read are both correct. Leave them alone.',
    ],
    reads: 'Two integers on the first line, then a single positive integer for the countdown.',
    prints: 'The two values in swapped order on two lines, then the countdown from that integer down to 1, one per line, then LIFTOFF.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int n = input.nextInt();',
      '',
      '    a = b;',
      '    b = a;',
      '    System.out.println(a);',
      '    System.out.println(b);',
      '',
      '    for (int i = n; i > 1; i--) {',
      '      System.out.println(i);',
      '    }',
      '    System.out.println("LIFTOFF");',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int n = input.nextInt();',
      '',
      '    int temp = a;',
      '    a = b;',
      '    b = temp;',
      '    System.out.println(a);',
      '    System.out.println(b);',
      '',
      '    for (int i = n; i >= 1; i--) {',
      '      System.out.println(i);',
      '    }',
      '    System.out.println("LIFTOFF");',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Write out what a and b hold after each of the two assignment lines runs. After the first line, the old value of a is gone: nothing else in the program is holding it. You need a third variable to park it in before you overwrite it.',
      'A countdown to 1 has to actually test 1. The condition i > 1 is already false when i reaches 1, so the body never runs for it.',
      'Only two lines change and one line is added. If you rewrote the loop as a while loop you did more work than the fix needed.',
    ],
    seo: 'AP CSA 2.1 debugging exercise: fix a variable swap that loses a value and a countdown loop that stops one number early.',
    cases: [
      { stdin: '3 8 5\n', hidden: 0 },
      { stdin: '10 20 1\n', hidden: 0 },
      { stdin: '-4 7 3\n', hidden: 1 },
      { stdin: '0 0 4\n', hidden: 1 },
      { stdin: '99 1 2\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.2', unit: 'unit-2',
    mode: 'program',
    name: 'Distributing the Not',
    title: 'Boolean Expressions',
    brief: 'Negating a compound condition is where confident students lose points. The rule is not the one arithmetic taught you: a not in front of a parenthesis does not simply move inside and stay the same shape.',
    task: [
      'The second line is supposed to print whether it is NOT true that a is greater than b AND b is greater than c. The code distributes the not across the and incorrectly, so it prints the wrong answer whenever exactly one of the two comparisons is true. Fix the expression.',
      'The third line is supposed to print whether a and c are equal while b is different from both. The code uses an or where the logic requires an and. Fix it.',
      'The first line is already correct. Do not change it.',
    ],
    reads: 'Three integers on one line.',
    prints: 'Three lines of true or false: whether all three are equal, whether it is not the case that a > b and b > c, and whether a equals c while b differs from both.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '',
      '    System.out.println(a == b && b == c);',
      '    System.out.println(!(a > b) && !(b > c));',
      '    System.out.println(a == c || a != b);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int a = input.nextInt();',
      '    int b = input.nextInt();',
      '    int c = input.nextInt();',
      '',
      '    System.out.println(a == b && b == c);',
      '    System.out.println(!(a > b && b > c));',
      '    System.out.println(a == c && a != b);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Build the truth table for a > b and b > c with two columns. The expression !(P && Q) is true in three of the four rows. The expression !P && !Q is true in only one of them. They agree only at the extremes, which is why small test inputs can hide this.',
      'When a not really does move inside a parenthesis, every and becomes an or and every or becomes an and. That is De Morgan. Here the simpler fix is to leave the not outside the whole thing.',
      'For the third line, read the sentence again: a equals c AND b is different. Two conditions that must both hold is never an or.',
    ],
    seo: 'AP CSA 2.2 debugging exercise: fix a negated compound boolean expression that distributes the not incorrectly across an and.',
    cases: [
      { stdin: '5 3 1\n', hidden: 0 },
      { stdin: '2 2 2\n', hidden: 0 },
      { stdin: '5 9 1\n', hidden: 1 },
      { stdin: '4 7 4\n', hidden: 1 },
      { stdin: '1 2 3\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.3', unit: 'unit-2',
    mode: 'program',
    name: 'Four Ifs Where One Chain Belongs',
    title: 'if Statements',
    brief: 'A chain of else if stops at the first branch that matches. Four separate ifs do not stop at all, and every one of them gets tested. The output tells you immediately which one you wrote, if you read it.',
    task: [
      'Run the program on a score of 95. It prints more than one letter grade. The bands were written as four independent if statements instead of one if / else if chain, so a high score satisfies several of them. Rewrite it as a single chain that prints exactly one letter.',
      'The PASS and FAIL line prints PASS for every score, including a 12. There is a stray semicolon immediately after the if condition, which ends the statement before the body. Find it and remove it.',
      'The distance-from-100 line at the bottom is correct.',
    ],
    reads: 'A single integer score from 0 to 100.',
    prints: 'Three lines: exactly one letter grade, then PASS or FAIL, then how far the score is from 100.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int score = input.nextInt();',
      '',
      '    if (score >= 90) {',
      '      System.out.println("A");',
      '    }',
      '    if (score >= 80) {',
      '      System.out.println("B");',
      '    }',
      '    if (score >= 70) {',
      '      System.out.println("C");',
      '    }',
      '    if (score >= 60) {',
      '      System.out.println("D");',
      '    }',
      '    if (score < 60) {',
      '      System.out.println("F");',
      '    }',
      '',
      '    if (score >= 60);',
      '    {',
      '      System.out.println("PASS");',
      '    }',
      '',
      '    System.out.println(100 - score);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int score = input.nextInt();',
      '',
      '    if (score >= 90) {',
      '      System.out.println("A");',
      '    } else if (score >= 80) {',
      '      System.out.println("B");',
      '    } else if (score >= 70) {',
      '      System.out.println("C");',
      '    } else if (score >= 60) {',
      '      System.out.println("D");',
      '    } else {',
      '      System.out.println("F");',
      '    }',
      '',
      '    if (score >= 60) {',
      '      System.out.println("PASS");',
      '    } else {',
      '      System.out.println("FAIL");',
      '    }',
      '',
      '    System.out.println(100 - score);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Trace a score of 95 by hand through all five ifs. It is at least 90, and also at least 80, and also at least 70, and also at least 60. Four of the five bodies run. else if is what makes the second test unreachable once the first one matched.',
      'A semicolon right after if (condition) is a complete, empty statement: the if runs it and does nothing. The block underneath is then just a plain block that always executes. This compiles without a single warning, which is what makes it worth recognizing on sight.',
      'The FAIL case has to print something. Once you remove the stray semicolon you still need an else, or a failing score prints no second line at all.',
    ],
    seo: 'AP CSA 2.3 debugging exercise: replace separate if statements with an else if chain and remove a stray semicolon after an if.',
    cases: [
      { stdin: '95\n', hidden: 0 },
      { stdin: '12\n', hidden: 0 },
      { stdin: '60\n', hidden: 1 },
      { stdin: '89\n', hidden: 1 },
      { stdin: '70\n', hidden: 1 },
      { stdin: '0\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.4', unit: 'unit-2',
    mode: 'program',
    name: 'Which if Does That else Belong To?',
    title: 'Nested if Statements',
    brief: 'An else attaches to the nearest unmatched if, not to the one lined up with it in the indentation. Indentation is a comment as far as the compiler is concerned. This program is indented to look right and behaves the other way.',
    task: [
      'Run the program with age 70 and member 0. It prints the wrong price. The else was written without braces on the outer if, so it binds to the INNER if instead of the outer one. Add the braces that make the else belong to the outer if.',
      'Seniors who are members should get the senior price, not the member price. Once the braces are fixed, check the order of the two tests inside the adult branch and make the senior test win.',
      'The reading of the two inputs is correct.',
    ],
    reads: 'An integer age, then 1 if the person is a member or 0 if not.',
    prints: 'One line: the ticket price. Under 13 pays 6. Age 65 and over pays 8 whether or not they are a member. Everyone else pays 10, or 9 if they are a member.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int age = input.nextInt();',
      '    int member = input.nextInt();',
      '',
      '    int price = 0;',
      '    if (age < 13)',
      '      if (member == 1)',
      '        price = 6;',
      '      else',
      '        price = 10;',
      '',
      '    if (age >= 13) {',
      '      if (member == 1) {',
      '        price = 9;',
      '      } else if (age >= 65) {',
      '        price = 8;',
      '      } else {',
      '        price = 10;',
      '      }',
      '    }',
      '',
      '    System.out.println(price);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int age = input.nextInt();',
      '    int member = input.nextInt();',
      '',
      '    int price = 0;',
      '    if (age < 13) {',
      '      price = 6;',
      '    } else if (age >= 65) {',
      '      price = 8;',
      '    } else if (member == 1) {',
      '      price = 9;',
      '    } else {',
      '      price = 10;',
      '    }',
      '',
      '    System.out.println(price);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Read the spec again for a child: under 13 pays 6, membership is not mentioned. The starter charges a non-member child 10. That whole inner if should not exist.',
      'Age 65 and over pays 8 whether or not they are a member. The starter tests membership first, so a 70 year old member is charged 9 and never reaches the senior test. Order matters in a chain: the more specific rule goes first.',
      'The cleanest fix collapses all of it into one four-branch chain on age, then membership. If you are still nesting an if inside an if, you are working harder than the spec requires.',
    ],
    seo: 'AP CSA 2.4 debugging exercise: fix a dangling else that binds to the wrong if and reorder ticket price conditions.',
    cases: [
      { stdin: '70 0\n', hidden: 0 },
      { stdin: '10 0\n', hidden: 0 },
      { stdin: '70 1\n', hidden: 1 },
      { stdin: '30 1\n', hidden: 1 },
      { stdin: '65 1\n', hidden: 1 },
      { stdin: '12 1\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.5', unit: 'unit-2',
    mode: 'program',
    name: 'The Guard That Does Not Guard',
    title: 'Compound Boolean Expressions',
    brief: 'The single ampersand and the double ampersand both compute the same answer for booleans. Only one of them protects the code on its right from ever running. When the thing on the right would divide by zero, that difference is a crash.',
    task: [
      'Run the program with a count of 0. It crashes with an ArithmeticException on a division by zero, even though the condition clearly checks that count is not zero first. The check uses a single ampersand, which evaluates BOTH sides before deciding. Change it to the operator that stops as soon as the left side is false.',
      'The in-range test is supposed to accept values from 10 through 99 inclusive. It uses an or, which accepts everything. Fix it so it means what it says.',
      'The reading of the inputs and the total are correct.',
    ],
    reads: 'An integer count, then that many integers, then one more integer to range check.',
    prints: 'Two lines: true if the count is not zero and the average is over 10, then true if the last value is between 10 and 99 inclusive.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int count = input.nextInt();',
      '',
      '    int total = 0;',
      '    for (int i = 0; i < count; i++) {',
      '      total = total + input.nextInt();',
      '    }',
      '    int check = input.nextInt();',
      '',
      '    System.out.println(count != 0 & total / count > 10);',
      '    System.out.println(check >= 10 || check <= 99);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int count = input.nextInt();',
      '',
      '    int total = 0;',
      '    for (int i = 0; i < count; i++) {',
      '      total = total + input.nextInt();',
      '    }',
      '    int check = input.nextInt();',
      '',
      '    System.out.println(count != 0 && total / count > 10);',
      '    System.out.println(check >= 10 && check <= 99);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'A single ampersand on two booleans is a real operator and it compiles fine. It just always evaluates the right side, so the division happens before the count check has any chance to protect it. The double ampersand short circuits: if the left side is false, the right side is never evaluated at all.',
      'This is the reason the null check and the length check are written in that order everywhere you will ever see them. The order only saves you if the operator honors it.',
      'For the range test, ask what check >= 10 || check <= 99 says about the value 5. It is not at least 10, but it is at most 99, so the or is satisfied. Every integer satisfies one side or the other.',
    ],
    seo: 'AP CSA 2.5 debugging exercise: replace a non short circuit ampersand causing a divide by zero and fix an or used as a range check.',
    cases: [
      { stdin: '0 5\n', hidden: 0 },
      { stdin: '3 20 30 40 50\n', hidden: 0 },
      { stdin: '2 1 2 5\n', hidden: 1 },
      { stdin: '0 100\n', hidden: 1 },
      { stdin: '4 11 11 11 11 99\n', hidden: 1 },
      { stdin: '1 5 9\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.6', unit: 'unit-2',
    mode: 'program',
    name: 'Equal Objects, Different Boxes',
    title: 'Comparing Boolean Expressions',
    brief: 'Two Strings that read the same on screen are not necessarily the same object, and == asks the object question, not the reading question. This is the single most common runtime surprise in the whole course, and it never produces an error message.',
    task: [
      'Run the program with the two words typed the same. The first line prints false anyway. The comparison uses == on two Strings, which compares whether they are the same object in memory, not whether they contain the same characters. Use the method that compares contents.',
      'The third line is supposed to print whether the two words are NOT both non-empty. The not was distributed across the and incorrectly. Fix it so it matches the sentence.',
      'The second line, which compares the lengths, is already correct. Lengths are ints, and == is right for ints.',
    ],
    reads: 'Two words separated by whitespace.',
    prints: 'Three lines of true or false: whether the words are the same text, whether their lengths are equal, and whether it is not the case that both words have length greater than zero.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String first = input.next();',
      '    String second = input.next();',
      '',
      '    String firstCopy = new String(first);',
      '',
      '    System.out.println(firstCopy == second);',
      '    System.out.println(first.length() == second.length());',
      '    System.out.println(!(first.length() > 0) && !(second.length() > 0));',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String first = input.next();',
      '    String second = input.next();',
      '',
      '    String firstCopy = new String(first);',
      '',
      '    System.out.println(firstCopy.equals(second));',
      '    System.out.println(first.length() == second.length());',
      '    System.out.println(!(first.length() > 0 && second.length() > 0));',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'new String(first) deliberately builds a second object holding the same characters. == compares the two references, and they point at different objects, so it is false no matter what the words say. equals compares character by character.',
      'You may have seen == appear to work on Strings in other programs. That is the string pool reusing one object for identical literals, and it is an optimization, not a guarantee. The moment a String comes from input or from new, the trick stops working. Never rely on it.',
      'For the third line, !(P && Q) is false only when both P and Q are true. !P && !Q is true only when both are false. Those are different in the two mixed cases: one empty word and one not.',
    ],
    seo: 'AP CSA 2.6 debugging exercise: fix a String compared with double equals instead of equals and a wrongly distributed logical not.',
    cases: [
      { stdin: 'cat cat\n', hidden: 0 },
      { stdin: 'apple pear\n', hidden: 0 },
      { stdin: 'a ab\n', hidden: 1 },
      { stdin: 'zebra zebra\n', hidden: 1 },
      { stdin: 'xy xy\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.7', unit: 'unit-2',
    mode: 'program',
    name: 'One Short, Every Time',
    title: 'while Loops',
    brief: 'This loop terminates, which is the good news. It just does the wrong amount of work, and the accumulator is updated in the wrong order, so both the count and the total are off. Neither mistake announces itself.',
    task: [
      'The program should print every integer from 1 through n. It stops at n - 1 because the condition excludes n itself. Fix the condition.',
      'The running total is supposed to be the sum of every number printed. The counter is increased BEFORE the value is added, so the first number is never counted and an extra one is. Move the update so the total matches what was printed.',
      'Do not convert this to a for loop. The exercise is about getting a while loop right.',
    ],
    reads: 'A single positive integer n.',
    prints: 'The integers from 1 through n, one per line, then the sum of those integers on the last line.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int i = 1;',
      '    int total = 0;',
      '    while (i < n) {',
      '      i++;',
      '      System.out.println(i);',
      '      total = total + i;',
      '    }',
      '    System.out.println(total);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int i = 1;',
      '    int total = 0;',
      '    while (i <= n) {',
      '      System.out.println(i);',
      '      total = total + i;',
      '      i++;',
      '    }',
      '    System.out.println(total);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Trace n = 3 on paper with two columns, i and total, writing a new row every time the body runs. The starter never prints 1 at all, because i is incremented before the first println. The first thing the body does should be the work, not the update.',
      'A counting while loop has a fixed shape: initialize before, test at the top, do the work, update last. Moving the update to the top is what skips the first value.',
      'i <= n and i < n differ by exactly one iteration. When the spec says through n inclusive, the condition has to let n into the body.',
    ],
    seo: 'AP CSA 2.7 debugging exercise: fix a while loop that stops one iteration early and updates its counter before doing the work.',
    // Hidden inputs are all LARGER than the largest visible one on purpose.
    // This program prints a cumulative sequence, so the output for any smaller n
    // is a prefix of the output for a larger n: with 5 visible, a hidden n=2
    // would have its whole expected output readable off the visible sample.
    // scripts/csa-debug-pages-csv.js checkNoLeak catches exactly that.
    cases: [
      { stdin: '5\n', hidden: 0 },
      { stdin: '1\n', hidden: 0 },
      { stdin: '10\n', hidden: 1 },
      { stdin: '6\n', hidden: 1 },
      { stdin: '7\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.8', unit: 'unit-2',
    mode: 'program',
    name: 'Zero Times Anything',
    title: 'for Loops',
    brief: 'An accumulator has to start at the identity for its operation. Starting a sum at zero is right and starting a product at zero is a guarantee that the answer is zero, forever, no matter how correct the rest of the loop is.',
    task: [
      'The factorial always prints 0. The product accumulator is initialized to 0, and zero multiplied by anything stays zero. Initialize it to the value that leaves a product unchanged.',
      'The count of even numbers is one too high whenever n itself is even. The loop bound uses <= where the spec says to stop below n. Fix the bound.',
      'The sum on the first line is correct. Notice that its accumulator starts at 0 and that this is right for a sum.',
    ],
    reads: 'A single integer n from 1 to 12.',
    prints: 'Three lines: the sum of 1 through n, then n factorial, then how many integers strictly below n are even.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int sum = 0;',
      '    for (int i = 1; i <= n; i++) {',
      '      sum = sum + i;',
      '    }',
      '    System.out.println(sum);',
      '',
      '    int product = 0;',
      '    for (int i = 1; i <= n; i++) {',
      '      product = product * i;',
      '    }',
      '    System.out.println(product);',
      '',
      '    int evens = 0;',
      '    for (int i = 1; i <= n; i++) {',
      '      if (i % 2 == 0) {',
      '        evens++;',
      '      }',
      '    }',
      '    System.out.println(evens);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int sum = 0;',
      '    for (int i = 1; i <= n; i++) {',
      '      sum = sum + i;',
      '    }',
      '    System.out.println(sum);',
      '',
      '    int product = 1;',
      '    for (int i = 1; i <= n; i++) {',
      '      product = product * i;',
      '    }',
      '    System.out.println(product);',
      '',
      '    int evens = 0;',
      '    for (int i = 1; i < n; i++) {',
      '      if (i % 2 == 0) {',
      '        evens++;',
      '      }',
      '    }',
      '    System.out.println(evens);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'The identity for addition is 0 because adding 0 changes nothing. The identity for multiplication is 1 for the same reason. An accumulator always starts at the identity of the operation it accumulates.',
      'The evens count says strictly below n. Test it with n = 4: the integers strictly below 4 are 1, 2 and 3, so the answer is 1. The starter includes 4 itself and reports 2.',
      'Only two characters change in the whole file. If your fix is longer than that, re-read the two lines named in the task.',
    ],
    seo: 'AP CSA 2.8 debugging exercise: fix a product accumulator initialized to zero and an off by one for loop bound.',
    cases: [
      { stdin: '5\n', hidden: 0 },
      { stdin: '4\n', hidden: 0 },
      { stdin: '1\n', hidden: 1 },
      { stdin: '10\n', hidden: 1 },
      { stdin: '7\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.9', unit: 'unit-2',
    mode: 'program',
    name: 'The Maximum That Cannot Be Negative',
    title: 'Implementing Selection and Iteration Algorithms',
    brief: 'Initializing a running maximum to zero works on every test case you are likely to type by hand, and fails the moment every value is negative. This is the classic silent bug: correct-looking code, correct output all week, wrong answer on the one input that matters.',
    task: [
      'Run the program on a list where every value is negative. It reports a maximum of 0, which is not even in the list. Initialize the running maximum from the data itself rather than from 0.',
      'The count of values equal to the maximum is always 1. The counter is being reset inside the loop instead of accumulated. Fix it so a list with three copies of the largest value reports 3.',
      'The minimum is computed correctly. Compare how it is initialized to how the maximum is initialized, and notice that the same fix pattern applies.',
    ],
    reads: 'An integer count of at least 1, then that many integers.',
    prints: 'Three lines: the largest value, the smallest value, and how many times the largest value appears.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int count = input.nextInt();',
      '    int[] data = new int[count];',
      '    for (int i = 0; i < count; i++) {',
      '      data[i] = input.nextInt();',
      '    }',
      '',
      '    int max = 0;',
      '    for (int i = 0; i < data.length; i++) {',
      '      if (data[i] > max) {',
      '        max = data[i];',
      '      }',
      '    }',
      '    System.out.println(max);',
      '',
      '    int min = data[0];',
      '    for (int i = 1; i < data.length; i++) {',
      '      if (data[i] < min) {',
      '        min = data[i];',
      '      }',
      '    }',
      '    System.out.println(min);',
      '',
      '    int times = 0;',
      '    for (int i = 0; i < data.length; i++) {',
      '      if (data[i] == max) {',
      '        times = 1;',
      '      }',
      '    }',
      '    System.out.println(times);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int count = input.nextInt();',
      '    int[] data = new int[count];',
      '    for (int i = 0; i < count; i++) {',
      '      data[i] = input.nextInt();',
      '    }',
      '',
      '    int max = data[0];',
      '    for (int i = 1; i < data.length; i++) {',
      '      if (data[i] > max) {',
      '        max = data[i];',
      '      }',
      '    }',
      '    System.out.println(max);',
      '',
      '    int min = data[0];',
      '    for (int i = 1; i < data.length; i++) {',
      '      if (data[i] < min) {',
      '        min = data[i];',
      '      }',
      '    }',
      '    System.out.println(min);',
      '',
      '    int times = 0;',
      '    for (int i = 0; i < data.length; i++) {',
      '      if (data[i] == max) {',
      '        times++;',
      '      }',
      '    }',
      '    System.out.println(times);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'A running maximum initialized to 0 is really claiming that 0 is a member of the list. If every value is negative, no element ever beats 0, and the loop reports a number that was never in the data. Start from data[0] and scan from index 1.',
      'times = 1 assigns; times++ accumulates. Assignment throws away everything counted so far, which is why the answer is always exactly 1 whenever the maximum appears at all.',
      'The minimum loop is the shape you want: initialize from the first element, then start the scan at index 1. Make the maximum match it.',
    ],
    seo: 'AP CSA 2.9 debugging exercise: fix a running maximum initialized to zero and a counter that assigns instead of accumulating.',
    cases: [
      { stdin: '4\n-5 -2 -9 -7\n', hidden: 0 },
      { stdin: '5\n3 9 4 9 1\n', hidden: 0 },
      { stdin: '3\n9 9 9\n', hidden: 1 },
      { stdin: '1\n-1\n', hidden: 1 },
      { stdin: '6\n-3 0 -8 0 -1 0\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.10', unit: 'unit-2',
    mode: 'program',
    name: 'The Empty Substring',
    title: 'Implementing String Algorithms',
    brief: 'substring takes a start and a stop, and the stop is not included. Passing the same number twice asks for the characters from position i up to but not including position i, which is nothing at all. The program prints blank lines and no error.',
    task: [
      'The program should print each character of the word on its own line. It prints blank lines instead: substring is called with the same index for both arguments, which always produces an empty String. Fix the second argument.',
      'The search should print the index where the target letter first appears, or -1 when it does not appear. The code adds 1 to the result unconditionally, which turns a not-found -1 into 0 and reports a real match at the wrong index. Remove the adjustment.',
      'The length line is correct.',
    ],
    reads: 'A word, then a single character to search for.',
    prints: 'The length of the word, then each character on its own line, then the index of the first occurrence of the search character or -1.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '    String target = input.next();',
      '',
      '    System.out.println(word.length());',
      '',
      '    for (int i = 0; i < word.length(); i++) {',
      '      System.out.println(word.substring(i, i));',
      '    }',
      '',
      '    System.out.println(word.indexOf(target) + 1);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    String word = input.next();',
      '    String target = input.next();',
      '',
      '    System.out.println(word.length());',
      '',
      '    for (int i = 0; i < word.length(); i++) {',
      '      System.out.println(word.substring(i, i + 1));',
      '    }',
      '',
      '    System.out.println(word.indexOf(target));',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'substring(a, b) returns the characters from index a up to but NOT including index b, so its length is b - a. When a and b are the same the length is zero. To get exactly one character starting at i, the stop has to be i + 1.',
      'indexOf returns -1 to mean not found, and -1 is a signal, not a position. Adding 1 to it produces 0, which is a perfectly valid index and reads as a match at the very front of the word. Never do arithmetic on a sentinel value.',
      'Check the not-found case deliberately. Searching a word for a letter it does not contain should print -1, not 0.',
    ],
    seo: 'AP CSA 2.10 debugging exercise: fix a substring call that returns an empty string and an indexOf result adjusted off by one.',
    cases: [
      { stdin: 'cat x\n', hidden: 0 },
      { stdin: 'hello l\n', hidden: 0 },
      { stdin: 'a a\n', hidden: 1 },
      { stdin: 'banana n\n', hidden: 1 },
      { stdin: 'zebra z\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.11', unit: 'unit-2',
    mode: 'program',
    name: 'The Inner Loop That Remembers',
    title: 'Nested Iteration',
    brief: 'An inner loop runs completely, from its start value to its end value, every single time the outer loop takes one step. When something that should be reset per row is initialized outside the outer loop instead, the rows quietly contaminate each other.',
    task: [
      'The grid should print rows rows of cols stars each. The inner loop uses the OUTER loop variable in its bound, so the rows come out ragged instead of rectangular. Fix the inner bound to use the column count.',
      'The per-row star count is supposed to reset at the start of each row. It is declared before the outer loop, so it keeps growing and every row reports a larger number than the one before. Move the declaration where it belongs.',
      'The total across all rows, printed at the very end, is correct and should stay outside the loops.',
    ],
    reads: 'Two integers, a number of rows and a number of columns, both at least 1.',
    prints: 'For each row, a line of that many stars followed by a line with the count of stars in that row. After all rows, the total number of stars printed.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int rows = input.nextInt();',
      '    int cols = input.nextInt();',
      '',
      '    int total = 0;',
      '    int rowCount = 0;',
      '    for (int r = 0; r < rows; r++) {',
      '      String line = "";',
      '      for (int c = 0; c <= r; c++) {',
      '        line = line + "*";',
      '        rowCount++;',
      '        total++;',
      '      }',
      '      System.out.println(line);',
      '      System.out.println(rowCount);',
      '    }',
      '    System.out.println(total);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int rows = input.nextInt();',
      '    int cols = input.nextInt();',
      '',
      '    int total = 0;',
      '    for (int r = 0; r < rows; r++) {',
      '      String line = "";',
      '      int rowCount = 0;',
      '      for (int c = 0; c < cols; c++) {',
      '        line = line + "*";',
      '        rowCount++;',
      '        total++;',
      '      }',
      '      System.out.println(line);',
      '      System.out.println(rowCount);',
      '    }',
      '    System.out.println(total);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'c <= r makes the number of stars depend on which row you are on, which is how you draw a triangle. A rectangle needs the inner bound to be the same every row, and that value is cols.',
      'Ask where each variable should be born. rowCount describes one row, so it is born at the top of a row and dies at the bottom: declare it inside the outer loop. total describes the whole grid, so it is born before any row starts.',
      'A variable declared inside a loop body is created fresh on every pass, which IS the reset. You do not need an explicit assignment back to zero if you put the declaration in the right place.',
    ],
    seo: 'AP CSA 2.11 debugging exercise: fix a nested loop using the outer variable as its bound and a counter that never resets per row.',
    cases: [
      { stdin: '3 4\n', hidden: 0 },
      { stdin: '1 5\n', hidden: 0 },
      { stdin: '4 2\n', hidden: 1 },
      { stdin: '2 2\n', hidden: 1 },
      { stdin: '5 1\n', hidden: 1 },
    ],
  },

  {
    lesson: '2.12', unit: 'unit-2',
    mode: 'program',
    name: 'Counting the Wrong Operations',
    title: 'Informal Run Time Analysis',
    brief: 'Run time analysis is counting, and counting is only meaningful if the counter sits where the work happens. Put the increment one block too high and a quadratic algorithm reports linear growth, which is exactly the wrong lesson.',
    task: [
      'The comparison counter for the nested pass is incremented in the OUTER loop body instead of the inner one, so it reports n instead of n squared. Move it to where the comparison actually happens.',
      'The single pass is supposed to report n - 1 comparisons, since comparing n items pairwise takes one fewer comparison than there are items. Its loop starts at 0 and reports n. Fix the start value.',
      'Do not change the loop bounds of the nested pass. The point of the exercise is that its count should grow like n squared, and it already visits the right pairs.',
    ],
    reads: 'A single integer n of at least 2.',
    prints: 'Two lines: the number of comparisons a single pass makes, then the number a nested pass makes.',
    starter: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int single = 0;',
      '    for (int i = 0; i < n; i++) {',
      '      single++;',
      '    }',
      '    System.out.println(single);',
      '',
      '    int nested = 0;',
      '    for (int i = 0; i < n; i++) {',
      '      nested++;',
      '      for (int j = 0; j < n; j++) {',
      '        int ignored = i + j;',
      '      }',
      '    }',
      '    System.out.println(nested);',
      '  }',
      '}',
    ].join('\n'),
    reference: [
      'import java.util.Scanner;',
      '',
      'public class Main {',
      '  public static void main(String[] args) {',
      '    Scanner input = new Scanner(System.in);',
      '    int n = input.nextInt();',
      '',
      '    int single = 0;',
      '    for (int i = 1; i < n; i++) {',
      '      single++;',
      '    }',
      '    System.out.println(single);',
      '',
      '    int nested = 0;',
      '    for (int i = 0; i < n; i++) {',
      '      for (int j = 0; j < n; j++) {',
      '        nested++;',
      '      }',
      '    }',
      '    System.out.println(nested);',
      '  }',
      '}',
    ].join('\n'),
    hints: [
      'Run the starter with n = 5 and then n = 10. The nested count doubles when n doubles, which is the signature of a linear algorithm. A genuinely nested loop should quadruple. That gap between the shape of the code and the shape of the number is the bug.',
      'Every statement inside the inner loop runs n times per outer pass, for n * n total. Every statement in the outer body runs only n times. Where you put the increment decides which number you are measuring.',
      'For the single pass, think about comparing neighbors in a list of n items: 5 items have only 4 neighboring pairs. Starting the loop at 1 instead of 0 is the whole fix.',
    ],
    seo: 'AP CSA 2.12 debugging exercise: move an operation counter into the inner loop so a nested pass reports quadratic instead of linear growth.',
    cases: [
      { stdin: '5\n', hidden: 0 },
      { stdin: '2\n', hidden: 0 },
      { stdin: '10\n', hidden: 1 },
      { stdin: '3\n', hidden: 1 },
      { stdin: '7\n', hidden: 1 },
    ],
  },
];

module.exports = { EXERCISES };
