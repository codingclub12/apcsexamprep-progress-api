PORT=4000
JWT_SECRET=CHANGE-THIS-TO-A-LONG-RANDOM-STRING-IN-PRODUCTION
DB_PATH=./progress.db
# Admin API (>= 20 chars). Unset means the /api/admin/* routes are disabled.
ADMIN_KEY=
# Shopify order webhook signing secret. Unset means /api/webhooks/shopify/* fails closed.
SHOPIFY_WEBHOOK_SECRET=
